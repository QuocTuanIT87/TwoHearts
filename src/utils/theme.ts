export const COLORS = {
  primary: "#5596e0",        // Main brand color
  primaryLight: "#e9f2fc",   // Light background overlay
  primaryDark: "#336fa7",    // Darker shade for buttons/headers
  accent: "#f368e0",         // Pink/Rose accent for love theme and milestones
  background: "#f8fbfe",     // Soft white/light-blue background
  surface: "#ffffff",        // Card/surface container color
  text: "#1e293b",           // Dark slate text
  textMuted: "#64748b",      // Gray text for timestamps
  border: "#e2e8f0",         // Light gray border
  borderActive: "#cbd5e1",   // Darker gray border for active text inputs
  success: "#10b981",        // Green for save actions and active statuses
  danger: "#ef4444",         // Red for delete/reset
  warning: "#f59e0b",        // Yellow for warnings
  lock: "#94a3b8",           // Neutral gray for 14-day lock status
  overlay: "rgba(0, 0, 0, 0.4)", // Modal overlay background
  lightGray: "#dddddd" //gray light
};

export const SIZES = {
  radiusSm: 8,
  radiusMd: 12,
  radiusLg: 16,
  paddingSm: 8,
  paddingMd: 18,
  paddingLg: 24,
};

export const SHADOWS = {
  light: {
    shadowColor: "#5596e0",
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.05,
    shadowRadius: 8,
    elevation: 2,
  },
  medium: {
    shadowColor: "#334155",
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.08,
    shadowRadius: 16,
    elevation: 4,
  },
};
